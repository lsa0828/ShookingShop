import TotalPrice from "../TotalPrice";

export default {
  title: 'Cart/TotalPrice',
  component: TotalPrice,
  args: {
  },
  render: () => {
    return (
      <TotalPrice />
    );
  }
};

export const Default = {
  args: {
  }
};