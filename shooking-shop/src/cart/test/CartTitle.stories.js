import CartTitle from "../CartTitle";

export default {
  title: 'Cart/CartTitle',
  component: CartTitle,
  args: {
  },
  render: () => {
    return (
      <CartTitle />
    );
  }
};

export const Default = {
  args: {
  }
};