import CartHeader from "../CartHeader";

export default {
  title: 'Cart/CartHeader',
  component: CartHeader,
  args: {
  },
  render: () => {
    return (
      <CartHeader />
    );
  }
};

export const Default = {
  args: {
  }
};