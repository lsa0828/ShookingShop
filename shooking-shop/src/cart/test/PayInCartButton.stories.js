import PayInCartButton from "../PayInCartButton";

export default {
  title: 'Cart/PayInCartButton',
  component: PayInCartButton,
  args: {
  },
  render: () => {
    return (
      <PayInCartButton />
    );
  }
};

export const Default = {
  args: {
  }
};